# Portable cold-start deps (build machine vendors into bin/runtime/win/python).
# Heavy OCR/PSD optional — not required to open the window.
pywebview>=5.0
pythonnet>=3.0
pystray>=0.19
Pillow>=10.0
bcrypt>=4.0
cryptography>=42.0
openpyxl>=3.1
redis>=5.0
psycopg2-binary>=2.9
# Branding grid builder: branding_publish.resolve_script_python(require_ijson=True)
ijson>=3.3
