# Gap audit 2026-07-21 — final synthesize (gapship + A + B + composers)

**Version:** `v3.1.0` (`apps/web/version.json`, `dam-version.js`, `runtime_config.py`)  
**Cache token:** `ship20260721v310`  
**Design Read:** B2B DAM Geex cockpit (Dobra Kaloria). Dense product/branding modals.

Sources merged: this worker gapship · Worker A `31f6c994` · Worker B `9ec6da1a` · viz-composer `a5fabf8f` · brand-composer `156723f5` · studioRow `db41aeb5` · mergeVar `36990489` (superseded by noSrcGrid).

Detailed zone logs: [`gap-audit-4h-worker-A.md`](gap-audit-4h-worker-A.md), [`gap-audit-4h-worker-B.md`](gap-audit-4h-worker-B.md), [`gap-audit-viz-composer-2026-07-21.md`](gap-audit-viz-composer-2026-07-21.md), [`gap-audit-brand-composer-2026-07-21.md`](gap-audit-brand-composer-2026-07-21.md).

## Final gap table

| ID | Claim | Final | Notes |
|----|-------|-------|-------|
| G0-UTF8 | `Pokaż` / `Włącz` / `Wyłącz` (no `Poka?`) | **Pass** | Worker B `compB20260721a` + gapship qmark repair |
| A1–A12 | Viz INDEX above studio, show-all rows+dedupe, tint, actions z40, chips nowrap, Shift-minus all-files, UK→GB UI, title gap, outline 1px | **Pass** | Worker A 12/12 |
| A13 | naming-dictionary UK≠Ukraina | **Pass** | `uk`→Wielka Brytania; `ua`→Ukraina |
| B1 | ONE WARIANTY strip, no all-files dup | **Pass*** | *Raster/video tiles only |
| **noSrcGrid** | PSD/PSB/AI/PDF **never** in `.variant-grid` | **Pass HARD** | `folderVariantsHtml`: always `return !isSourceVariantFile(v)`; sources = SourceMount only. Worker B CDP `["PSD","JPG"]` **superseded / invalid** |
| B2 | materialMode: no fake studio axes | **Pass** | |
| B3 | Shift-minus −20% (21px), Shift-gated | **Pass** | |
| B7 | paintAssoc not blocked by enrich hang | **Pass** | sync paint first |
| studioRow | PERSPEKTYWA one flex row | **Pass** | `studioRow20260721b` |
| adminGate | no ADMIN without session | **Pass** | parent `adminGate20260721a` |

## HARD rules locked this ship

1. **Sources out of grid:** PSD/PSB/AI/PDF/… only in action-bar SourceMount — never WARIANTY tiles.  
2. **UTF-8 only** for `apps/web` writes (Python `write_bytes` / editor) — never PowerShell Get/Set-Content.  
3. **UK/GB/EN** = English market; Ukraina = `UA` only.

## Screenshot / CDP proof (representative)

- Viz: ORZESZKI / Kulki / jab-ko — Worker A passes 1–12  
- Branding: BLIX / material packs — raster tiles only after noSrcGrid  
- `#vizShowAll` label bytes `Pokaż wszystkie`

## Abandoned / N/A

- PSD as WARIANTY tiles (explicitly forbidden)  
- Shift-minus on product INDEX strip (index pickers, not assoc remove)
