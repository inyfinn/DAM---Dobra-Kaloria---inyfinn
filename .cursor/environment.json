{
  "name": "Synology MCP — Cloud SSH",
  "install": "pip install --user -r bin/apps/desktop/requirements.txt\ncommand -v composer >/dev/null 2>&1 && composer install --no-interaction --working-dir=bin/apps/api || true\ncommand -v npm >/dev/null 2>&1 && npm install --prefix bin/apps/api || true\ngit clone --depth 1 https://github.com/inyfinn/synology-mcp.git /tmp/synology-mcp 2>/dev/null || true\nbash /tmp/synology-mcp/scripts/cloud-agent-ssh-setup.sh 2>/dev/null || bash bin/scripts/ops/setup-cloud-ssh-syno.sh || true",
  "start": "bash /tmp/synology-mcp/scripts/cloud-agent-ssh-setup.sh --verify-only 2>/dev/null || bash bin/scripts/ops/setup-cloud-ssh-syno.sh --verify-only || true"
}
